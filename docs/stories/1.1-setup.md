# Story 1.1: Project Setup & Hello World

**Trạng thái:** Approved 🟢

## 1. User Story
* **Là:** Developer
* **Tôi muốn:** Khởi tạo dự án Python, cài đặt các thư viện cần thiết và cấu hình kết nối cơ bản với Telegram.
* **Để:** Bot có thể chạy được (online hoặc local) và phản hồi tin nhắn đầu tiên ("Hello") khi người dùng gõ `/start`.

## 2. Acceptance Criteria (Tiêu chí chấp nhận)
1. Repository được khởi tạo với cấu trúc thư mục chuẩn (theo Architecture).
2. File `requirements.txt` chứa đầy đủ các thư viện lõi: `python-telegram-bot`, `sqlalchemy`, `fastapi`, `uvicorn`, `python-dotenv`.
3. File `Dockerfile` được cấu hình tối ưu (sử dụng `python:3.11-slim`) để sẵn sàng deploy lên Render.
4. Bot chạy được và phản hồi tin nhắn `/start` với nội dung chào mừng.
5. Biến môi trường (`TELEGRAM_TOKEN`) được load an toàn từ file `.env` (local).

## 3. Tasks (Nhiệm vụ cụ thể)
* [x] `git init` và tạo file `.gitignore` (loại bỏ `.env`, `__pycache__`).
* [x] Tạo cấu trúc thư mục: `src/bot`, `src/database`, `src/services`.
* [x] Viết file `src/config.py` để load biến môi trường.
* [x] Viết file `src/main.py` làm entry point:
    * Thiết lập `ApplicationBuilder` (PTB).
    * Đăng ký handler cho lệnh `/start`.
* [x] Viết hàm `start_command` trong `src/bot/handlers.py` trả lời: *"Xin chào! Tôi là NoTocBot. Gõ /help để xem hướng dẫn."*
* [x] Tạo `Dockerfile` và `render.yaml`.

## 4. Dev Notes (Ghi chú kỹ thuật)
* **Tech Stack:** Python 3.11+.
* **Framework:** `python-telegram-bot` (PTB) v20+.
* **Lưu ý:** Chưa cần kết nối Database ở Story này. Tập trung vào việc Bot "sống" được đã.